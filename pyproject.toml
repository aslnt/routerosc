[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "routerosc"
version = "0.3.0"
description = "Asynchronous Python client for MikroTik RouterOS API"
readme = "README.md"
license = {file = "LICENSE"}
authors = [{name = "aslnt"}]
requires-python = ">= 3.10"
